services:
  - type: web
    name: your-app-name
    env: python
    pythonVersion: "3.12"   # ← forces 3.12
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn ndu_portal.wsgi:application"